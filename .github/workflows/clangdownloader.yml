name: Download and Upload via 0x0.st

on:
  workflow_dispatch:

jobs:
  download-and-upload:
    runs-on: ubuntu-latest

    steps:
      - name: Install aria2
        run: sudo apt-get update && sudo apt-get install -y aria2

      - name: Download Clang from GitHub Release
        run: |
          mkdir -p downloads
          aria2c -x 16 -s 16 -d downloads -o Clang.tar.gz \
            "https://github.com/ZyCromerZ/Clang/releases/download/22.0.0git-20250919-release/Clang-22.0.0git-20250919.tar.gz"

      - name: Upload to 0x0.st
        id: upload
        run: |
          url=$(curl -F "file=@downloads/Clang.tar.gz" https://0x0.st)
          echo "download_url=$url" >> $GITHUB_OUTPUT

      - name: Send link to Telegram
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
        run: |
          curl -s -X POST "https://api.telegram.org/bot${BOT_TOKEN}/sendMessage" \
            -d "chat_id=${CHAT_ID}" \
            -d "text=✅ File Clang berhasil diupload:\n${{ steps.upload.outputs.download_url }}"
